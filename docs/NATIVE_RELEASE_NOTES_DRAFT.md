# Pre-native hardening release notes (draft)

- Clarifies Update queue as the immediate community action.
- Renames Join queue to optional Start wait timer for verified actual-wait samples.
- Adds self-service account deletion and public deletion route.
- Fixes guest contributor-metrics 401 noise.
- Adds HTTP-200 static entry points for known routes.
- Improves large-list rendering and mobile touch targets.
- Prepares remote ODA index configuration and native WebView CORS source.
- Makes synthetic UX scanner failures visible.
- Refreshes account/native release documentation.

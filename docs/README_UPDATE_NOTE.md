# README merge follow-up

When the pre-native hardening PR merges, update the root README production baseline to the merged PR number and include:

- Update queue remains the primary immediate community contribution.
- The former Join queue / I'm in line feature is an optional verified **Start wait timer** for actual-wait samples.
- Public/account static routes now have HTTP-200 entry points.
- Self-service account deletion and `/account-deletion` are available.
- Unsigned guest metric 401 noise is removed.
- Native address-index base/CORS preparation and mobile rendering/touch-target hardening are included.
- `docs/PRE_NATIVE.md` and `docs/NATIVE_RELEASE_CHECKLIST.md` define the remaining native release gates.

The README is the project handoff and should not claim this branch is production until the PR actually merges and post-deploy checks pass.

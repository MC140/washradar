The zero-cost address autocomplete source is Statistics Canada's National Address Register (NAR). Download the current PUMF ZIP, filter to GTA municipalities/census subdivisions, normalize address fields, and measure the resulting indexed size before loading production `address_points`. Do not bulk-load all Ontario/Canada addresses into the Supabase free database. If GTA rows plus indexes threaten the 500 MB database allowance, generate FSA-partitioned static autocomplete files and serve them from GitHub Pages while using Supabase only as a small selected-address cache.